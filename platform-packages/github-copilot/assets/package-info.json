{
  "id": "github-copilot",
  "displayName": "GitHub Copilot",
  "template": "remote-react-sidecar-adapter-v1",
  "owner": "cockpit-tools",
  "notes": [
    "Business UI, tabs, OAuth, switching, quota refresh, OpenCode sync, and VS Code instance management are supplied by this platform package.",
    "Core Shell owns only lifecycle controls, runtime gate, Host API, and adapter runner."
  ]
}
